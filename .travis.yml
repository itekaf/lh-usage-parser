language: node_js
node_js:
  - "8"
install: npm install
script:
  - npm run lint
  - npm run test

jobs:
  include:
    - stage: semantic release
      if: branch = master AND type = push
      node_js: stable
      script: npm run semantic-release
